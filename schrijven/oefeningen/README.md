# Oefeningen

Timed practice attempts, one file per day: `YYYY-MM-DD.md`. Write cold (~5 min), then compare
against the relevant `voorbeelden/` model text and note what to fix. Recurring mistakes go
into a GitHub Issue (`fout` template, label `skill:schrijven`) rather than repeated here.
